# This root file is only a quick reference — the apps read their own .env files:
#   backend/.env   (copy from backend/.env.example)
#   frontend uses Vite's dev proxy, no .env required for local dev
#
# See backend/.env.example for the full list of variables (database, JWT
# secrets, Super Admin seed credentials, SMTP).
